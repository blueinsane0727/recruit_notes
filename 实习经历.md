# 实习经历梳理

## 软件最差运行时间分析工具

- 针对WCET工具，基于 ARM Cortex-M3/M4 平台设计轻量化插桩模块，利用 DWT 周期计数器和环形缓冲区实现时间戳采集，并通过 UART 输出至 PC 端进行解析和执行时间统计。

### 项目背景与目标

- **你们这个WCET分析工具具体是做什么的？输入是什么，输出是什么？**

这个工具是应用在高安全领域，需要对程序的运行时间进行严格控制的分析工具，客户公司的程序员使用这个工具时需导入程序源码或者可执行文件，选定分析的入口，工具会根据汇编码进行静态分析得到理论上的最差运行时间，再通过插桩进行混合分析，最终能够输出两种分析模式下的报告，函数调用图，内部控制流图等帮助程序员去优化程序性能。

- **插桩模块在整个WCET工具链中处于什么位置？和静态分析部分是什么关系？**

在这个工具中分为PC端软件和运行在嵌入式设备上的插装模块两个部分，这个插桩模块就是负责在底层设备上记录时间戳并发送给PC端解析的部分，它是建立在静态分析基础上的，静态分析得到的最差路径和最差运行时间往往悲观不具有实际运行的说服力，因此利用这个模块能够计算在最差运行路径上的实际运行时间，起到一个验证和补充的作用。

- **什么是WCET？测量法和静态分析法有什么区别？**

在软件最差运行时间分析中，有三种分析方法。一种是静态分析，针对目标码进行反汇编，根据汇编指令、时钟周期等计算最差路径和最差运行时间，这种结果是悲观的，实际运行往往不会有这么慢，但这也能找出一条值得分析的最差路径；第二种是动态分析，这种方法旨在实际测量各种路径上的运行时间，但我们无法知道自己测量的是否是最差的，一般采用以大量的测量次数和尝试各种路径增加结果的可信度；

而第三种方法就是混合分析，这种方法融合了静态分析和动态分析的优点，先通过静态分析得到一条最差路径，再使用动态分析去实际测量最差路径的实际运行时间，使得得到的结果是基于实际的而且并不那么悲观。

本项目就是基于这种混合分析方法的软件最差运行时间分析工具的插桩模块，针对工具利用静态分析方法得出的最差路径去定点插桩，测量出真实的最差时间。

- **你们要测的是函数级、基本块级还是指令级执行时间？**

提供了函数级和基本块级测量的选择接口，可以在PC端工具界面根据需求进行选择，工具会调用脚本在源码中加入插桩相关文件并在相应位置插入接口。

- **这个插桩模块最终给谁用？是内部调试工具还是交付给客户？**

公司业务就是为客户进行定制化软件工具的，因此是交付给客户，由客户团队使用这个工具来改善他们的产品开发。

- **你在这个模块中具体负责哪些代码？独立完成还是团队协作？**

主要负责ARM Cortex M3、M4平台的适配，因为之前有这个平台的开发经验，然后在这个平台上完成时间戳采集串口发送等嵌入式平台上的代码编写。

- **项目周期多长？你投入了多少时间？代码量大概多少？**

大概两三周，这两三周上班全在写这个模块，代码量大概一千多行

### DWT与时间戳采集

- **DWT是什么？Cortex-M3/M4里的DWT主要有哪些功能？**

DWT 是 数据观察点与跟踪单元，属于 ARM 调试组件。Cortex-M3/M4 的 DWT 主要有：比较器用于 watchpoint、PC 采样、以及 CYCCNT 周期计数器。我们主要用 CYCCNT 来读 CPU 周期数，做高精度时间戳。

- **为什么选择DWT的CYCCNT，而不是SysTick、通用定时器或GPIO翻转+示波器？**

SysTick 通常被 RTOS 或系统时基占用；通用定时器需要额外配置，可能占用外设；GPIO 翻转+示波器适合离线测量，但无法在程序内部自动记录大量插桩点。DWT CYCCNT 是 CPU 周期级计数器，读取开销极小，不需要中断，适合轻量级插桩。

- **DWT周期计数器的时钟源是什么？和CPU主频是什么关系？**

CYCCNT 一般随 CPU 主频计数，一个周期加一。所以时间换算就是：周期数 / CPU 主频 = 秒。比如 72MHz，一个周期约 13.9ns。

- **如何使能DWT CYCCNT？需要配置哪些寄存器？追问：DEMCR、DWT_CTRL、DWT_CYCCNT分别怎么设置？**

典型步骤：
    使能调试跟踪：CoreDebug->DEMCR |= CoreDebug_DEMCR_TRCENA_Msk;
    使能 CYCCNT：DWT->CTRL |= DWT_CTRL_CYCCNTENA_Msk;
    清零计数器：DWT->CYCCNT = 0;
    之后就可以读 DWT->CYCCNT。

- **Cortex-M3和M4的DWT有差异吗？移植时需要注意什么？**

M3 和 M4 都有 DWT，基本寄存器兼容。差异主要在芯片厂商实现、主频、调试使能方式。
移植时主要改主频定义、UART 引脚、DWT 使能代码，以及确认芯片是否支持 CYCCNT。

- **CYCCNT是几位计数器？在你们的主频下多久溢出一次？**

CYCCNT 是 32 位。比如 72MHz，2^32 / 72e6 ≈ 59.6 秒溢出一次

- **溢出怎么处理？如何扩展成更高位的时间戳？**

DWT 没有溢出中断，通常做法是：维护一个 64 位软件计数器，每次读 CYCCNT 时判断是否比上次小，如果变小说明回绕，高 32 位加一。或者定期在定时器中断里检查并累加。最终时间戳 = 高 32 位 << 32 | 当前 CYCCNT。

- **时间戳是绝对周期数还是相对时间？如何换算成微秒/毫秒？**

我们记录的是绝对周期数，PC 端解析后做差得到相对周期数，再除以主频换算成时间。比如周期差 72000，主频 72MHz，就是 1ms。

- **低功耗模式、睡眠或调试器连接状态会影响DWT计数吗？**

会。进入睡眠或深度低功耗后，CPU 时钟可能停止，CYCCNT 可能不计数；调试器连接状态也可能影响 TRCENA。所以测量时一般要保持芯片正常运行

- **如果DWT不工作，你怎么排查？追问：TRCENA没开？计数器没使能？芯片不支持？调试器影响？**

先查 DEMCR.TRCENA 是否置 1，再查 DWT_CTRL.CYCCNTENA 是否置 1，然后看 DWT_CYCCNT 是否变化。如果不变，检查芯片是否支持 DWT、调试器是否影响、时钟是否使能。也可以换一块板子对比。

- **时间戳采集的精度和误差大概是多少？怎么验证？**

精度理论上是一个 CPU 周期，误差主要来自插桩代码本身的开销、中断干扰、UART 传输延迟。验证可以用 GPIO 翻转 + 示波器对比，或者用已知延时的循环测试。

- **有没有和示波器、逻辑分析仪或GPIO翻转做对比测试？**

我们用 GPIO 翻转配合示波器做过简单对比，确认 DWT 读数和示波器测出的时间在同一量级

### 插桩模块设计与开销

- **插桩点是怎么选的？手动指定还是工具自动插入？**

插桩点由 PC 端工具根据静态分析得到的最差路径自动生成，支持函数级和基本块级。工具调用脚本在源码中插入插桩接口，嵌入式端只负责提供接口实现。也可以手动指定入口函数。

- **函数入口/出口、分支、循环怎么插桩？嵌套调用怎么处理？**

函数入口和出口各插一个点，记录开始/结束时间戳；分支在分支目标处插桩；循环在循环体入口和出口插桩，记录迭代次数。嵌套调用用唯一 ID 配对。

- **插桩代码如何做到“轻量级”？追问：代码体积、RAM占用、执行周期、关中断时间分别是多少？**

热路径全部用宏展开，没有函数调用，时间戳读取贴在窗口边界上，窗口内没有插桩指令。中断隔离用 BASEPRI，而不是全局关中断。把优先级低于阈值的中断屏蔽掉，只让更高优先级的中断，这样测量窗口内不会被普通中断打断，保证测到的是非中断任务的纯代码耗时。内存静态分配，无动态分配，无标准库依赖。

- **插桩本身会不会改变被分析程序的行为？如何测量并扣除这部分开销？**

会改变，但设计上尽量把影响限制在固定、可测量的范围内。时间上，窗口内只读两次 DWT_CYCCNT，中间没有插桩指令，但两次读之间至少隔一条指令，所以会引入约 2 个周期的固定偏高。测量开销用 wcet_calibrate：空窗连续读两次 CYCCNT，取最小值，得到 offset_cycles，大约 2 个周期。

- **插桩代码如何避免被编译器优化掉？追问：volatile、内存屏障、内联汇编、__NOP？**

避免被优化主要靠三点。第一，DWT 寄存器定义成 volatile 指针。第二，热路径宏里加了编译器屏障 __asm volatile("" ::: "memory")，防止窗口外的记账代码被挪进窗口，也防止窗口内用户代码被挪出去。第三，BASEPRI 的读写用内联汇编，带 memory clobber，保证编译器和 CPU 都不会乱序。

- **函数内联、尾调用、递归会对执行时间统计造成什么影响？**

这三种情况都会影响插桩点和静态地址的对齐。每个计数器只有一个 start 字段，所以递归或嵌套调用同一函数时，内层会覆盖外层 start，外层算出来的时间就错了。尾调用可能把 END 优化掉，配对失败。内联可能让 addr 标签消失或指向调用者，静态工具按地址对齐就失败。

我们的处理方式是：测量在单独的“特性化固件”里做，对被测函数禁用内联和尾调用优化，或者用 -O0 编译；递归函数不直接插桩，而是拆成非递归段分别测，或者用栈式结构保存多个 start。

- **中断里会插桩吗？中断嵌套、中断延迟怎么处理？**

一般不在中断里插桩，因为会干扰实时性。如果必须，需要特殊标记，并考虑中断嵌套和延迟。我们主要测主程序路径。

- **插桩点的时间戳如何配对？比如函数开始和结束如何对应？**

每个插桩点有唯一 ID，进入和退出成对。

- **如何保证多插桩点的时间戳顺序正确？**

单核上 DWT 是全局周期计数，读出的时间戳天然有序。只要写入缓冲区时保持顺序，PC 端按序解析即可。

- **插桩模块的可移植性如何？换一款Cortex-M芯片要改哪些地方？**

DWT 在 Cortex-M3/M4 通用，主要改主频定义、UART 引脚和波特率、DWT 使能代码。如果换 M0/M7，要确认 DWT 支持情况

- **有没有考虑对中断响应时间、实时性的影响？**

考虑了，这个模块的定位就是“只测非中断任务的耗时”，中断干扰、系统级响应时间不归它管，由另一套方法单独分析。所以测量时用 BASEPRI 把优先级低于阈值的中断屏蔽掉，保证窗口内测到的是纯代码时间。代价是测量期间这些中断的响应被推迟，推迟时间等于被测代码段的执行时间。高优先级中断比如 NMI、HardFault 不受影响，仍然可以打断。

### 环形缓冲区与并发

- **为什么需要环形缓冲区？直接通过UART发不行吗？**

UART 发送慢，时间戳产生快，直接发会阻塞程序，改变被测代码行为。环形缓冲区可以缓冲数据，由 DMA 慢慢发送。

- **环形缓冲区是单生产者单消费者，还是多生产者多消费者？**

通常是单生产者（插桩点）单消费者（UART 发送）。如果中断和主循环都插桩，就是多生产者，需要加锁或关中断。

- **读写指针怎么定义？空和满怎么判断？**

用 head 和 tail。空：head == tail；满：(head + 1) % size == tail。

- **缓冲区满了怎么办？覆盖旧数据、丢弃新数据，还是阻塞？**

我们选择丢弃新数据并设置溢出标志，避免阻塞被测程序。PC 端看到溢出标志就知道数据不完整。

- **如果丢数据，PC端怎么知道？有没有序号、溢出计数或错误标记？**

有。每条记录可以带序号，或者维护一个溢出计数器，PC 端解析时检查序号是否连续，不连续就报丢包。

- **中断和主循环同时访问缓冲区时，如何保证线程安全？追问：关中断、原子操作、内存屏障、无锁SPSC？**

单生产者单消费者可以用无锁环形缓冲区，配合内存屏障。

- **缓冲区大小怎么定的？依据是什么？**

根据 UART 波特率和最大时间戳产生速率算。比如 115200bps，每帧 10 字节，约 11.5KB/s；缓冲区 2KB 可以缓冲约 0.17 秒的数据。

- **每个时间戳记录占多少字节？结构体怎么设计？**

typedef struct {
    char     name[WCET_NAME_MAX_LEN]; // 32 字节
    uint32_t addr;                    // 4 字节
    uint32_t start;                   // 4 字节
    uint32_t max_cycles;              // 4 字节
    uint32_t call_count;              // 4 字节
} wcet_counter_t;

- **大量插桩点时，缓冲区和UART带宽会不会成为瓶颈？怎么算的？**

会。如果插桩点很多，每秒产生几万条记录，UART 可能来不及。需要提高波特率或减少插桩点。

### UART传输与PC端解析

- **UART波特率是多少？为什么选这个波特率？**

常用 115200 或 921600。115200 兼容性好，921600 带宽高。我们根据数据量选择，保证不丢包。

- **UART是中断发送、DMA发送还是阻塞发送？为什么？**

使用DMA发送，避免阻塞cpu

- **数据帧格式是什么？二进制还是文本？有没有帧头、长度、校验、序号？**

二进制，序号和数据

- **UART丢包或误码怎么处理？有没有重传、校验或流控？**

丢包一般不重传，因为测量数据可以丢弃

- **PC端解析工具用什么写的？Python吗？**

- **PC端如何对齐插桩点、计算执行时间？**

根据插桩点 ID 和进入/退出标记配对，时间戳做差，再除以主频换算成时间。同时对基本块也有做地址对其，在数据中加入地址方便pc解析

- **执行时间统计包括哪些指标？最大值、最小值、平均值、方差？**

包括最大值，最小值

- **如何识别异常数据？有没有滤波、去极值？**

WCET 关注最大值，所以异常值要保留并分析原因。

- **数据量有多大？实时传输还是先存到Flash再导出？**

实时传输，通过 UART 发给 PC。数据量根据插桩点数量和运行时间决定。

- **PC端解析和统计结果如何反馈给WCET工具？**

解析成json，wcet工具通过读json文件知道数据

### WCET与验证

- **测量法只能得到“观测到的最大执行时间”，你怎么能说是WCET？**

我们不是纯测量法，是混合分析。先用静态分析得到最差路径，再在这条路径上定点插桩测量实际时间，所以测量的是最差路径上的实际执行时间，比纯动态分析可信。

- **你们如何构造最坏情况输入？测试用例怎么设计？**

根据静态分析得到的最差路径，构造能触发该路径的输入。测试用例由人工设计，确保覆盖最差路径。

- **测了多少次？覆盖了哪些路径？如何确定覆盖充分？**

测了多次，覆盖函数级和基本块级路径。通过代码覆盖率和路径覆盖率判断，确保最差路径被多次测量。

- **有没有和静态WCET分析结果对比？误差多大？**

有。静态结果通常更悲观，动态实测结果更小。误差取决于路径和硬件，可能几倍到几十倍。

- **如何验证插桩后统计出来的执行时间是正确的？**

用已知执行时间的测试代码验证，比如空循环、已知延时函数，对比 DWT 读数和示波器。

- **有没有做过边界测试？比如最大负载、最频繁中断、缓冲区溢出？**

有。测试过缓冲区满、最大插桩点数量，确保系统稳定。

- **你们的结果有没有支撑后续算法迭代？具体提供了什么数据？**

有。提供了最差路径的实际执行时间，帮助算法调整静态分析的悲观程度，优化 WCET 估计。

- **如果让你重新设计这个插桩模块，你会改什么？**

会改进缓冲区管理，增加更可靠的溢出检测；使用别的方式传输数据

- **这个模块最大的技术难点是什么？你怎么解决的？**

中断的分析问题

- **这个项目里你最大的收获和不足是什么？**

收获是理解了 WCET 混合分析和嵌入式高精度时间测量。不足是项目周期短，有些边界情况没覆盖全。

### 最可能被追问的10个高频问题

- **DWT CYCCNT怎么初始化？溢出怎么处理？**

- **为什么用DWT而不是SysTick或通用定时器？**

- **环形缓冲区怎么保证中断和主循环并发安全？**

- **缓冲区满了怎么办？丢数据怎么标记？**

- **UART波特率、数据量、丢包怎么处理？**

- **插桩开销有多大？怎么测量和扣除？**

- **插桩代码怎么防止被编译器优化？**

- **测量法如何得到WCET？和静态分析怎么结合？**

- **Cortex-M3和M4移植差异是什么？**

- **你个人具体做了什么？遇到的最大困难是什么？**


## 编码错误检查工具

**为静态编码错误检查工具搭建基于 Python 的 MISRA C:2012 自动化测试框架，并编写/维护 37 条 MISRA C 规则测试用例**。

### 一、项目背景与定位
1. 这个静态编码错误检查工具是自研、外购还是内部工具？主要解决什么问题？

这个是为客户公司研发供客户公司使用的工具，主要解决检查代码有无违反misra c规则的问题，misra c有143条规则，如果人工审核的话无法完全检出且工作量巨大耗时耗力。

2. 工具的输入是什么？输出格式是什么？命令行、API 还是插件形式？

输入是工程文件，输出可以使用gui界面查看也能使用命令行

3. 为什么要搭自动化测试框架？原来人工怎么测？痛点是什么？

人工检测需要手动导入工程文件或者输入命令行，并且要一条一条核对测试点是否全部检出，是否被正确检出，而自动化框架只需要运行脚本就能自动调用工具检测并一一比对生成结果和报告

4. 你在项目中的角色是什么？独立负责还是团队协作？周期多久？

团队协作，包括框架和测试用例一共花了差不多两个月的时间

5. “工具鉴定测试”具体指什么？和这个自动化测试框架是什么关系？

我们需要让客户和评审觉得我们的工具具有说服力，能够对各种违反规则的行为检出，同时不误报，这就是工具鉴定包，本质就是一个测试集，它包括了这个测试框架和测试用例

6. 这个项目和第一条“嵌入式插桩模块”是同一个实习项目还是两个项目？

这是两个项目

7. 简历里“准确率由 76% 提升至 94%”是指这个静态检查工具，还是 WCET 工具？统计口径是什么？

是指这个检查工具，一开始使用这些测试用例对工具进行测试的时候工具的正确检出率只有76%，剩下的都漏报了，研发团队根据我们的初步测试结果对工具进行完善，在我离职之前，工具的检出率已经达到了94%，剩下还有一些漏报还需要再对工具进行迭代

### 二、自动化测试框架设计与 Python 实现
8. 框架整体架构是什么？有哪些模块？数据流是怎样的？
9. 测试用例“自动发现”具体怎么实现？按目录、文件名、装饰器还是 pytest 收集？
10. 测试用例的元数据怎么存？YAML、JSON、Python 字典还是数据库？
11. 如何调用被测工具？用 subprocess、命令行、SDK 还是 API？
12. 工具执行超时、崩溃、返回码非零时怎么处理？
13. 如何解析工具输出？XML、JSON、CSV、SARIF 还是自定义格式？
14. 如果工具输出格式升级或字段变化，框架如何适配？
15. 批量执行是串行还是并行？多进程、多线程还是任务队列？
16. 如何做环境隔离？临时目录、工作目录、文件清理怎么管理？
17. 结果归档成什么格式？文件、SQLite、日志还是数据库？
18. 报告生成用什么库？Jinja2、HTML、XML、JUnit 还是 Allure？
19. 报告里包含哪些字段？通过率、失败详情、漏报、误报、规则维度统计？
20. 框架如何与 Git/SVN、CI/CD 集成？是否支持定时回归？
21. 新增一条规则或新增一个工具版本时，框架如何扩展？
22. 测试框架自身有没有测试？怎么保证框架逻辑正确？

### 三、37 条 MISRA C 规则与测试用例设计
23. 为什么选择这 37 条 MISRA C 规则？来源是什么？谁定的？



24. 这 37 条覆盖了哪些类别？指令类、规则类、必需、建议、可判定、不可判定？
25. 每条规则大概写了多少测试用例？正例、反例、边界用例怎么分布？
26. 测试文件如何组织？命名规范是什么？是否有多文件、头文件、宏、条件编译场景？
27. 预期结果怎么定义？写在文件里、YAML 里还是 Python 断言里？
28. 预期结果由谁评审？如何保证预期结果本身符合 MISRA C:2012？
29. 如何处理规则配置、偏差、抑制注释、规则组开关？
30. 测试用例是否考虑 C 标准、编译器差异、平台差异？
31. 同一行有多个违规时，预期结果怎么表达？
32. 举例说明：某一条 MISRA 规则，你怎么设计正例和反例？
33. 如果让你现场写一条新规则的测试用例，你会怎么写？
34. 37 条规则中，哪一条最难测？为什么？
35. 新增一条规则的标准流程是什么？从查文档到入库要几步？

### 四、判定机制、漏报误报与统计指标
36. “规则诊断 ID”是什么？是工具内部 ID、规则号，还是映射后的 ID？
37. 诊断 ID 和 MISRA 规则号如何建立映射？映射表怎么维护？
38. 判定通过/失败时，只匹配诊断 ID，还是也匹配行号、列号、消息、严重级别？
39. 漏报怎么定义？预期有诊断但工具没报？
40. 误报怎么定义？预期没有诊断但工具报了？
41. 多报、少报、错报、位置偏移怎么处理？
42. “逐规则识别漏报和误报”具体怎么实现？输出规则级混淆矩阵吗？
43. 准确率、精确率、召回率、F1 分别怎么算？分母是什么？
44. 76% 到 94% 是按用例数、规则数还是诊断数统计？有没有加权？
45. 如果工具升级后诊断 ID 变了，历史结果怎么对比？
46. 如果同一规则在同一行报多个诊断，判定逻辑怎么处理？
47. 发现漏报或误报后，如何提交 bug？如何复现？如何跟踪修复？
48. 统计结果如何归档、可视化和回溯？

### 五、工程化、协作与难点
49. 这个框架自动化后，节省了多少人工时间？有没有量化收益？
50. 你和工具开发、测试、项目组怎么协作？需求怎么对齐？
51. 遇到最大的技术挑战是什么？怎么解决的？
52. 有没有遇到工具崩溃、超时、环境不一致、路径编码等问题？怎么处理？
53. 如果测试用例数量从几十条扩展到几千条，框架怎么扩展？
54. 如何做增量测试？只测变更规则或变更代码？
55. 如果让你重新设计这个框架，你会改什么？
56. 你认为当前框架还有哪些不足？下一步优化方向是什么？
57. 这个项目里你最有成就感的点是什么？
58. 你从 MISRA C:2012 和静态分析工具测试中学到了什么？

### 六、现场/压力追问
59. 给你一条新的 MISRA 规则，从 0 到 1 加入框架，你怎么做？
60. 如果工具输出格式改为 SARIF，你的框架要改哪些地方？
61. 如果要求支持 Windows 和 Linux 双平台，怎么保证一致性？
62. 如果要求 10 万条用例在 10 分钟内跑完，你怎么设计？
63. 现场写一个 pytest 参数化测试用例，调用外部工具并断言诊断 ID。
64. 解释一下你设计的“基于诊断 ID 的判定机制”伪代码。
65. 如果预期结果和实际结果都有多个诊断，如何做集合匹配？


