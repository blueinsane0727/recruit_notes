# 实习经历梳理

## 软件最差运行时间分析工具

- 针对WCET工具，基于 ARM Cortex-M3/M4 平台设计轻量化插桩模块，利用 DWT 周期计数器和环形缓冲区实现时间戳采集，并通过 UART 输出至 PC 端进行解析和执行时间统计。

### 项目背景与目标

- **你们这个WCET分析工具具体是做什么的？输入是什么，输出是什么？**

这个工具是应用在高安全领域，需要对程序的运行时间进行严格控制的分析工具，客户公司的程序员使用这个工具时需导入程序源码或者可执行文件，选定分析的入口，工具会根据汇编码进行静态分析得到理论上的最差运行时间，再通过插桩进行混合分析，最终能够输出两种分析模式下的报告，函数调用图，内部控制流图等帮助程序员去优化程序性能。

- **插桩模块在整个WCET工具链中处于什么位置？和静态分析部分是什么关系？**

在这个工具中分为PC端软件和运行在嵌入式设备上的插装模块两个部分，这个插桩模块就是负责在底层设备上记录时间戳并发送给PC端解析的部分，它是建立在静态分析基础上的，静态分析得到的最差路径和最差运行时间往往悲观不具有实际运行的说服力，因此利用这个模块能够计算在最差运行路径上的实际运行时间，起到一个验证和补充的作用。

- **什么是WCET？测量法和静态分析法有什么区别？**

在软件最差运行时间分析中，有三种分析方法。一种是静态分析，针对目标码进行反汇编，根据汇编指令、时钟周期等计算最差路径和最差运行时间，这种结果是悲观的，实际运行往往不会有这么慢，但这也能找出一条值得分析的最差路径；第二种是动态分析，这种方法旨在实际测量各种路径上的运行时间，但我们无法知道自己测量的是否是最差的，一般采用以大量的测量次数和尝试各种路径增加结果的可信度；

而第三种方法就是混合分析，这种方法融合了静态分析和动态分析的优点，先通过静态分析得到一条最差路径，再使用动态分析去实际测量最差路径的实际运行时间，使得得到的结果是基于实际的而且并不那么悲观。

本项目就是基于这种混合分析方法的软件最差运行时间分析工具的插桩模块，针对工具利用静态分析方法得出的最差路径去定点插桩，测量出真实的最差时间。

- **你们要测的是函数级、基本块级还是指令级执行时间？**

提供了函数级和基本块级测量的选择接口，可以在PC端工具界面根据需求进行选择，工具会调用脚本在源码中加入插桩相关文件并在相应位置插入接口。

- **这个插桩模块最终给谁用？是内部调试工具还是交付给客户？**

公司业务就是为客户进行定制化软件工具的，因此是交付给客户，由客户团队使用这个工具来改善他们的产品开发。

- **你在这个模块中具体负责哪些代码？独立完成还是团队协作？**

主要负责ARM Cortex M3、M4平台的适配，因为之前有这个平台的开发经验，然后在这个平台上完成时间戳采集串口发送等嵌入式平台上的代码编写。

- **项目周期多长？你投入了多少时间？代码量大概多少？**

大概两三周，这两三周上班全在写这个模块，代码量大概一千多行

### DWT与时间戳采集

- DWT是什么？Cortex-M3/M4里的DWT主要有哪些功能？


- 为什么选择DWT的CYCCNT，而不是SysTick、通用定时器或GPIO翻转+示波器？

- DWT周期计数器的时钟源是什么？和CPU主频是什么关系？

- 如何使能DWT CYCCNT？需要配置哪些寄存器？追问：DEMCR、DWT_CTRL、DWT_CYCCNT分别怎么设置？

- Cortex-M3和M4的DWT有差异吗？移植时需要注意什么？

- CYCCNT是几位计数器？在你们的主频下多久溢出一次？

- 溢出怎么处理？如何扩展成更高位的时间戳？

- 时间戳是绝对周期数还是相对时间？如何换算成微秒/毫秒？

- 低功耗模式、睡眠或调试器连接状态会影响DWT计数吗？

- 如果DWT不工作，你怎么排查？追问：TRCENA没开？计数器没使能？芯片不支持？调试器影响？

- 时间戳采集的精度和误差大概是多少？怎么验证？

- 有没有和示波器、逻辑分析仪或GPIO翻转做对比测试？


### 插桩模块设计与开销

插桩点是怎么选的？手动指定还是工具自动插入？

函数入口/出口、分支、循环怎么插桩？嵌套调用怎么处理？

插桩代码如何做到“轻量级”？
追问：代码体积、RAM占用、执行周期、关中断时间分别是多少？

插桩本身会不会改变被分析程序的行为？如何测量并扣除这部分开销？

插桩代码如何避免被编译器优化掉？
追问：volatile、内存屏障、内联汇编、__NOP？

函数内联、尾调用、递归会对执行时间统计造成什么影响？

中断里会插桩吗？中断嵌套、中断延迟怎么处理？

插桩点的时间戳如何配对？比如函数开始和结束如何对应？

如何保证多插桩点的时间戳顺序正确？

插桩模块的可移植性如何？换一款Cortex-M芯片要改哪些地方？

有没有考虑对中断响应时间、实时性的影响？

### 环形缓冲区与并发

为什么需要环形缓冲区？直接通过UART发不行吗？

环形缓冲区是单生产者单消费者，还是多生产者多消费者？

读写指针怎么定义？空和满怎么判断？

缓冲区满了怎么办？覆盖旧数据、丢弃新数据，还是阻塞？

如果丢数据，PC端怎么知道？有没有序号、溢出计数或错误标记？

中断和主循环同时访问缓冲区时，如何保证线程安全？
追问：关中断、原子操作、内存屏障、无锁SPSC？

缓冲区大小怎么定的？依据是什么？

每个时间戳记录占多少字节？结构体怎么设计？

大量插桩点时，缓冲区和UART带宽会不会成为瓶颈？怎么算的？

### UART传输与PC端解析

UART波特率是多少？为什么选这个波特率？

UART是中断发送、DMA发送还是阻塞发送？为什么？

数据帧格式是什么？二进制还是文本？有没有帧头、长度、校验、序号？

UART丢包或误码怎么处理？有没有重传、校验或流控？

PC端解析工具用什么写的？Python吗？

PC端如何对齐插桩点、计算执行时间？

执行时间统计包括哪些指标？最大值、最小值、平均值、方差？

如何识别异常数据？有没有滤波、去极值？

数据量有多大？实时传输还是先存到Flash再导出？

PC端解析和统计结果如何反馈给WCET工具？

### WCET与验证

测量法只能得到“观测到的最大执行时间”，你怎么能说是WCET？

你们如何构造最坏情况输入？测试用例怎么设计？

测了多少次？覆盖了哪些路径？如何确定覆盖充分？

有没有和静态WCET分析结果对比？误差多大？

如何验证插桩后统计出来的执行时间是正确的？

有没有做过边界测试？比如最大负载、最频繁中断、缓冲区溢出？

你们的结果有没有支撑后续算法迭代？具体提供了什么数据？

如果让你重新设计这个插桩模块，你会改什么？

这个模块最大的技术难点是什么？你怎么解决的？

这个项目里你最大的收获和不足是什么？

### 最可能被追问的10个高频问题

DWT CYCCNT怎么初始化？溢出怎么处理？

为什么用DWT而不是SysTick或通用定时器？

环形缓冲区怎么保证中断和主循环并发安全？

缓冲区满了怎么办？丢数据怎么标记？

UART波特率、数据量、丢包怎么处理？

插桩开销有多大？怎么测量和扣除？

插桩代码怎么防止被编译器优化？

测量法如何得到WCET？和静态分析怎么结合？

Cortex-M3和M4移植差异是什么？

你个人具体做了什么？遇到的最大困难是什么？