# C语言
## 关键字

**static**：
- 修饰局部变量时，会改变变量的生命周期，使得该变量不在随着函数调用结束而销毁，可以用于对该函数的调用次数计数或在使用大数组时防止函数栈溢出
- 修饰全局变量和函数时，会改变链接属性，将其作用域限定在定义它们的文件中，可以用于实现封装

**const**：
- 修饰局部变量时，该变量被赋值后就不能再修改
- 修饰指针
	常量指针 ` int const *p ` ：指针指向的值是常量，指针指向的值不能改变，指针指向可改变
	指针常量 ` int *const p `：指针本身是常量，不能改变指向的地址，可以修改地址中保存的值

**extern**:
- 引用另一个文件中的变量或函数

`#ifndef` :
 - 判断在当前文件中是否定义过某个宏，如果没有定义，代码会继续执行，如果定义过，后面的代码会被跳过

`#define`：
- 定义一个宏

`#endif`：
- 用来结束一个条件编译块，标记防止重复包含的结束位置

**volatile**：
- 防止编译器优化，使系统从内存中读取数据，常用于中断程序访问到的变量、多线程中的共享变量、并行设备的硬件寄存器

**register**：
- 建议编译器将指定变量存储在CPU寄存器中，提高程序的运行效率

## 概念/区别

### 全局变量与局部变量的区别

**作用域 声明 生命周期 存储位置**

全局变量：
- 在函数外部声明，整个程序都可以访问
- 声明时会被默认初始化
- 整个程序执行期间都存在
- 存储在data段或BSS段

局部变量：
 - 在函数内部声明，只能在所属函数中访问
 - 声明时没有默认初始化，需要手动赋值才能使用
 - 生命周期短，只在所属函数执行期间存在
 - 存储在栈

### typedef 和define的区别

define：
- 是预处理指令，在预处理时做简单的替换
- 不做正确性检查
- 无作用域限制

typedef：
- 关键字，编译时处理
- 具有类型检查的功能
- 有作用域限制

###  `#include<>` 和 `#include""` 的区别

使用 `#include<>` ：
- 编译器会先在系统的标准头文件中查找，找不到则报错，一般用于包含系统头文件

使用 `#include""` ：
- 在当前文件的相对路径或指定的绝对路径中寻找头文件，用于包含用户自定义的头文件或其他非系统头文件

### strcpy和memcpy的区别

strcpy：
- 字符串拷贝，复制以'\0'结尾的字符串
- 仅适用于字符串
- 存在缓冲区溢出风险

memcpy：
- 字节级内存拷贝
- 适用于任意类型的内存
- 不处理结束符，按指定字节数进行拷贝
- 存在更高的溢出风险

### sizeof和strlen区别

sizeof：
- 是一个运算符
- 在编译时得到结果
- 用于计算内存字节数
- 适用于任何数据类型
- 会包含'\0'结尾字符

strlen：
- 是一个函数
- 在运行时被调用
- 用于计算字符串的字符个数
- 只适用于字符串
- 不包含结束符

### 数组名与指针区别
![[Pasted image 20260727215620.png]]

### 数组指针与指针数组区别

数组指针`int (*p)[n]`：
- 是一个指针，指向数组的指针

指针数组`int *p[n]`:
- 是一个数组，数组中存放指针

### 指针函数与函数指针区别

指针函数：
- 返回一个指针的函数

函数指针：
- 是一个指针变量指向一个函数，常用于回调函数

### 三个内存分配函数

malloc：用于分配指定大小的内存块（不会初始化）

calloc：用于分配内存并初始化为0

realloc：用于重新调整已分配内存块的大小

### 大端和小端

大端：将高位字节存储在低地址位置，低位地址存储在高地址位置

小端：将低位字节存储在低地址位置，高位地址存储在高地址位置

### 引用和指针

指针：
- 是一个变量，存储的是另一个变量的内存地址
- 可以修改指向
- 可以指向空地址

引用：
- 是变量的别名，为一个已存在的变量创建了新的名字，共享一块内存区域
- 无法更改绑定对象
- 不能指向空地址

## 内存管理

栈区：
- 存放函数的局部变量、函数参数值以及函数调用和返回时的相关信息
- 系统自动分配和释放内存
- 从高地址向低地址增长

堆区：
- 动态分配的内存空间
- 用于存放程序运行时动态申请的内存，即malloc、free或操作系统提供的接口申请和释放的内存
- 从低地址向高地址增长

BSS段：
- 存放未初始化的全局变量和静态变量、初始化为0的全局变量和静态变量

data段：
- 存放已初始化且不为0的全局变量和静态变量

代码段：
- 存放程序的机器指令，通常是只读的

### 内存泄漏

内存泄漏是指程序运行过程中，分配的内存没有被及时释放，导致这部分内存无法再被程序使用
如new和malloc申请资源后，没有用delete和free释放

### 内存碎片

内存碎片是指在内存管理过程中，由于内存空间分配和释放的不规则性，产生的零散、不连续的内存区域，分为外部碎片和内部碎片。
- 外部碎片：在内存中由于太小无法满足新进程对内存的申请需求而无法被分配
- 内部碎片：已经分配给进程的内存空间中，部分内存没有被实际使用的情况。

## 编译原理相关

### .c文件变成可执行文件的四个过程

- **预处理**：执行预处理指令，展开头文件，替换宏定义，注释通常被删除，条件编译被选择，处理后变为.i文件
- **编译**：把 C 代码转换为目标 CPU 对应的汇编代码，同时进行类型检查、语法检查、优化、生成汇编代码，输出.s文件
- **汇编**：将汇编代码转换为机器码，生成目标文件。输出.o文件
- **链接**：合并多个.o文件，解析外部符号和函数调用，分配代码、变量的最终地址，生成中断向量表，链接库。输出可执行文件

# 单片机

## 内核相关

### **关键寄存器**

R0 ~ R12：通用寄存器

R13：栈指针 SP

R14：链接寄存器 LR

R15：程序计数器 PC

xPSR：程序状态寄存器

PRIMASK / BASEPRI / FAULTMASK：中断屏蔽

CONTROL：线程模式权限和栈选择

**通用寄存器**
用于存储临时数据、计算结果、函数参数等
- R0-R3存储函数参数，同时R0也用于存储返回值
	R0 在函数刚进入时用于接收第一个参数；函数即将返回时，第一个参数通常已经不再需要，所以 R0 可以复用来保存返回值。
- R4-R11通常用于保存局部变量、中间计算结果等
- R12是过程调用内部临时寄存器，属于易失寄存器，函数调用后其值通常不保证保留

**特殊寄存器 R13、R14、R15**
- **R13：SP 栈指针寄存器** 用于管理栈，栈用于存储调用信息、局部变量、返回地址等。M3内核有两个栈指针：MSP（主栈指针）和PSP（进程栈指针）
- **R14：LR 链接寄存器** 保存函数返回地址
	执行`BL function`指令：跳转到function，将返回地址保存到LR
	执行`BX LR`指令：跳转到LR保存的地址
	异常发生时，LR 保存 EXC_RETURN 特殊值，用于指导硬件异常返回时恢复哪个栈、返回哪个模式。
- **R15：PC 程序计数器** 保存下一条将要执行的指令地址

**程序状态寄存器xPSR**
xPSR是组合状态寄存器，由三部分组成：
- APSR：运算状态标志位
- IPSR：当前异常号
- RPSR：执行状态

Cortex-M3 没有硬件专用的 BP 寄存器。BP 或 FP 是编译器约定的帧指针，Thumb 模式下通常使用 R7，某些 ARM/AAPCS 场景下使用 R11。SP 固定是 R13。是否使用帧指针由编译器和优化选项决定。

**Cortex-M3 进入异常时硬件会自动压栈 R0、R1、R2、R3、R12、LR、PC 和 xPSR，而 R4 到 R11 需要软件保存。这是 Cortex-M3 异常处理和 RTOS 任务切换的核心机制。**

#### MSP和PSP的区别与作用

- MSP是主堆栈指针，是处理器上电复位后默认使用的栈指针，主要用于中断、异常的栈操作。
	启动第一个任务前会获取MSP初值，确保MSP是合法、正确的栈地址，避免异常触发后系统崩溃
- PSP是进程堆栈指针，是专门为任务或线程模式准备的，用于管理每个任务的私有栈。
MSP专注于中断与异常处理，保证中断或异常期间栈空间独立干净不会被任务破坏。PSP专注于任务执行，每个任务独有的栈由PSP管理，任务之间互不干扰。
这样做的优点是：任务栈和中断栈分离，便于上下文切换和异常管理。

### **上电复位流程**
```
上电 / 复位
    ↓
硬件根据 BOOT 配置选择启动区域
    ↓
将启动区域映射到 0x00000000
    ↓
读取 0x00000000：设置 MSP 初始值
    ↓ 从向量表的第一个地址读取初始 MSP 值，从第二个地址读取 Reset_Handler 地址并跳转执行。
读取 0x00000004：跳转到 Reset_Handler
    ↓
启动文件执行
    ├── 复制 .data：将Flash中的初始值拷贝到SRAM
    ├── 清零 .bss 段
    ├── 初始化 C/C++ 运行环境
    └── 调用 SystemInit()：配置系统时钟相关内容
    ↓
进入 main()
    ↓
初始化外设、开启中断、执行主循环或启动 RTOS
```

### **CPU工作过程**

**1.取指令**

程序计数器PC保存下一条指令的地址，CPU会使用这个地址去内存中提取指令并将其加载到指令寄存器中。

**2.解码指令**

指令解码器会解析指令，分析出操作码和操作数。

**3.执行指令**

CPU根据解码后的信息执行指令中指定的操作。

**4.访问内存**

如果指令需要访问内存（写入或读取数据），会使用ALU或其他硬件单元来进行地址计算，完成对指定内存位置的访问。

**5.写回结果**

如果指令产生了结果，CPU会将结果写回寄存器。

### **中断发生的过程**

**产生中断请求**

外设产生事件，如定时器更新时间、串口接收到数据，会设置外设状态寄存器中的中断标志位，向NVIC发出中断请求

**NVIC判断是否响应**

NVIC会根据以下条件判断是否响应：
- 外设中断是否使能；
- NVIC 中对应 IRQ 是否使能；
- 当前中断优先级；
- 当前 CPU 是否屏蔽中断；
- 是否有更高优先级中断正在执行；
- 是否处于更高优先级异常处理中。

**CPU响应中断并保存现场**

当 CPU 接受中断后，Cortex-M 会自动完成异常进入操作，不需要软件手动保存所有寄存器。

硬件自动压栈 R0、R1、R2、R3、R12、LR、PC 和 xPSR。这些寄存器组成异常栈帧，用于保存被中断程序的执行现场。

随后 CPU 会：
- 切换到 Handler Mode；
- 使用 MSP；
- 更新状态寄存器；
- 将当前返回地址保存到 LR 的特殊异常返回值中；
- 根据中断优先级决定是否允许更高优先级中断嵌套。

**找到中断服务函数**

CPU 根据中断号计算向量表中对应的入口地址。

**执行中断服务函数**

中断服务函数 ISR 一般负责：
1. 判断具体的中断来源；
2. 读取或处理数据；
3. 清除中断标志；
4. 通知主程序或 RTOS；
5. 尽快退出中断。

**返回并恢复现场**

硬件识别到该特殊值后，会自动：
- 从栈中恢复 `R0~R3`、`R12`、LR、PC、xPSR；
- 恢复被中断程序的运行状态；
- 继续执行原来被中断的位置。

### **中断注意事项**

一个外设事件通常要经过多个使能层级：
```
外设事件发生
    ↓
外设中断标志位置位
    ↓
外设中断源使能
    ↓
NVIC IRQ 使能
    ↓
CPU 全局中断未屏蔽
    ↓
进入 ISR
```

**中断服务函数应尽量短小，通常只做快速处理.**

不建议在 ISR 中执行：
- 长时间循环；
- 延时函数；
- printf；
- 阻塞式通信；
- 复杂计算；
- 调用可能阻塞的 RTOS API。

**为什么中断需要尽快执行**
- 减少延迟，提高实时性
- 避免资源占用：中断处理通常具有比普通线程更高的执行优先级，如果长时间占用CPU资源，可能会导致其他任务无法即时执行。
- 保持硬件设备的稳定性：硬件设备在处理中断时，可能会产生内部状态的变化，如果执行时间过长，可能会导致设备超时或失效。

## 通信协议

## 外设

## 存储

## 其他

### MCU为什么用C语言而不是C++
- 内存开销：c++的虚函数异常处理会引入大量内存
- 破坏实时性：拷贝构造函数运算符重载会导致代码的可执行时间不可控


# freertos

## 任务调度

## 任务通信

## 内存管理

# 操作系统基本知识

# Linux